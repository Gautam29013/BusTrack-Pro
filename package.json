{
  "name": "bustrackpro-root",
  "version": "1.0.0",
  "description": "Root project for BusTrackPro",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:all": "docker-compose up postgres mongo redis -d && concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
